<div class="container">
        <h1 class="text-center">
            PDF Generation
        </h1>
        <table class="table">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Age</th>
                    <th>Country</th>
                </tr>
            </thead>
            <tbody>
                {{#each rows}}
                    <tr>
                        <td>{{this.item_id}}</td>
                        <td>{{this.item_id}}</td>
                        <td>{{this.item_id}}</td>
                    </tr>
                {{/each}}
            </tbody>
        </table>
    </div>
